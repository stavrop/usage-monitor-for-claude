# Build artifacts — release binaries live in GitHub Releases, never in the tree
*.app/
ClaudeUsage.app/
*.zip
*.o
*.dSYM/
*.swiftmodule

# macOS
.DS_Store

# Logs / local
/tmp/claudeusage.*.log
*.log

# Secrets — never commit tokens or signing keys
*.token
*.p8
*.p12
.env
.env.*

# Local LaunchAgent with a machine-specific absolute path
launchd/*.local.plist
